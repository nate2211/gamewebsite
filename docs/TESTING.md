# Validation

Run:

```powershell
npm run validate
npm run build
```

The validator checks the modular folder rules, key gameplay modules, static asset counts, and presence of the production build. CRA performs Babel compilation, module resolution, and ESLint validation during the optimized build.
